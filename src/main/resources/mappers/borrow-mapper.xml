<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Borrow">
	<insert id="insert">
		insert into borrowboard values(#{seq},#{writer},#{title},#{category},#{contents},#{price},#{address},sysdate)
	</insert>

	<!-- 게시글 번호 -->
	<select id="getSeq" resultType="int">
		select TalentBoard_seq.nextval from dual
	</select>
  
	<!-- 전체 검색 지역 & 타이틀 & 내용 -->
	<select id="toSearch" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where address like '%'||#{search}||'%' or title like '%'||#{search}||'%' or contents like '%'||#{search}||'%' or category like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
	
	<!-- 주소 검색 -->
	<select id="toAddress" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where address like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
	
	<!-- 타이틀 검색 -->
	<select id="toTitle" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where title like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
	
	<!-- 컨텐츠 검색 -->
	<select id="toCategory" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where category like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
	
	<!-- 컨텐츠 검색 -->
	<select id="toContents" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where contents like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
	
	<!-- 검색값이 null일 경우 전체목록 나오게 -->
	<select id="toList" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard order by seq desc) A where ROWNUM <= #{endNum}) where rnum >= #{startNum}]]>
	</select>
		
	<!-- 검색 지역 & 타이틀 & 컨텐츠 카운트 -->
	<select id="allList" resultType="int">
		select count(*) from borrowboard
	</select>
		
	<!-- 검색 지역 & 타이틀 & 컨텐츠 카운트 -->
	<select id="numAllList" resultType="int">
		select count(*) from borrowboard where address like '%'||#{search}||'%' or title like '%'||#{search}||'%' or contents like '%'||#{search}||'%' or category like '%'||#{search}||'%'
	</select>
	
	<!-- 검색 지역 카운트 -->
	<select id="numAddress" resultType="int">
		select count(*) from borrowboard where address like '%'||#{search}||'%' 
	</select>
	
	<!-- 검색 지역 카운트 -->
	<select id="numCategory" resultType="int">
		select count(*) from borrowboard where category like '%'||#{search}||'%' 
	</select>
	
	<!-- 검색 타이틀 카운트 -->
	<select id="numTitle" resultType="int">
		select count(*) from borrowboard where title like '%'||#{search}||'%'
	</select>
	
	<!-- 검색 컨텐츠 카운트 -->
	<select id="numContents" resultType="int">
		select count(*) from borrowboard where contents like '%'||#{search}||'%'
	</select>
	
</mapper>