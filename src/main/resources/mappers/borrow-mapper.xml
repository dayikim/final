<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Borrow">
  
	<!-- 검색 지역 & 타이틀 -->
	<select id="toSearch" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * from borrowboard where address1 like '%'||#{search}||'%' or title like '%'||#{search}||'%' order by seq desc) A where ROWNUM <= #{endRowNum}) where rnum >= #{startRowNum}]]>
	</select>
	
	<!-- 검색이 null일 경우 전체목록 나오게 -->
	<select id="toList" resultType="kh.spring.dto.BorrowDTO">
		<![CDATA[select * from (select rownum as rnum, A.* from (select * FROM borrowboard order by seq desc) A where ROWNUM <= #{endRowNum}) where rnum >= #{startRowNum}]]>
	</select>
		
	<!-- 검색 지역 & 타이틀 카운트 -->
	<select id="numSearch" resultType="int">
		select count(*) from borrowboard where address1 like '%'||#{search}||'%' or title like '%'||#{search}||'%'
	</select>
	
	<!-- 검색 지역 & 타이틀 카운트 -->
	<select id="numList" resultType="int">
		select count(*) from borrowboard
	</select>
	
</mapper>