<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="TbFiles">
<insert id="upload">
insert into TBoardfiles values(TBoardfiles_seq.nextval,#{oriName},#{sysName},#{parentSeq},sysdate)
</insert>

	<!-- 사진 리스트(부모) -->
	<select id="tlsSelectAll" resultType="kh.spring.dto.TBoardFilesDTO">
		select * from lendfiles where parentSeq=#{value} order by seq asc
	</select>
	
	<!-- 사진 이름 -->
	<select id="tlsSysName" resultType="kh.spring.dto.TBoardFilesDTO">
		select sysname from lendfiles where seq=#{value}
	</select>
	
	<!-- 사진 삭제 -->
	<delete id="tlsDlete">
		delete from lendfiles where seq=#{value}
	</delete>
	
	<!-- 사진 리스트(부모) -->
	<select id="tlrSelectAll" resultType="kh.spring.dto.TBoardFilesDTO">
		select * from lendfiles where parentSeq=#{value} order by seq asc
	</select>
	
	<!-- 사진 이름 -->
	<select id="tlrSysName" resultType="kh.spring.dto.TBoardFilesDTO">
		select sysname from lendfiles where seq=#{value}
	</select>
	
	<!-- 사진 삭제 -->
	<delete id="tlrDlete">
		delete from lendfiles where seq=#{value}
	</delete>
	

<!-- <select id="selectAll" resultType="kh.spring.dto.FilesDTO">
select * from files where parent = #{parent}
</select>
 -->
</mapper>